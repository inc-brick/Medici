import connector from "../connector";

const resource = '/artist'

export default {
  fetchArtistInfo() {
    // connector.get(resource)
    //   .then(response => {
    //     if (response.status === 200) {
    //       this.$store.state['fetchArtistInfo'] = response.data
    //       return response.data
    //     }
    //   })
  }
}
